import Form from '@/components/forms/Form';

const App = () => {
    return (
        <main className="flex justify-center items-center h-screen">
            <Form />
        </main>
    );
};

export default App;
